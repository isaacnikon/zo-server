Client-verified item notes used by the server.

- `selected-items.json`: curated item ids and notes verified from installed client scripts or nearby matching asset dumps.

Current focus:
- `21116` is the quest item checked by the installed `script.gcg` flow for `Back to Earth`.
- The installed client script shows: `macro_GetItemCount(21116)==0` before Matt says "The wood is in your pack!!!".
